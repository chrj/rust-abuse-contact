[workspace]
# The crate reads answers but cannot fetch them yet, so it does not belong on
# crates.io. release-plz still writes the changelog, raises the version, tags and
# makes the GitHub release. Turn this on when the transport layer lands.
publish = false
# Compare the public API against the released version and refuse a version number
# that is too small for the change.
semver_check = true
changelog_update = true
git_release_enable = true
git_release_draft = false
git_tag_enable = true

[changelog]
protect_breaking_commits = true

commit_parsers = [
    { message = "^feat", group = "Added" },
    { message = "^fix", group = "Fixed" },
    { message = "^perf", group = "Changed" },
    { message = "^refactor", group = "Changed" },
    { message = "^doc", group = "Documentation" },
    { message = "^test", skip = true },
    { message = "^ci", skip = true },
    { message = "^chore", skip = true },
    { message = "^build", skip = true },
    { message = "^style", skip = true },
]
